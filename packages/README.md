# Packages

- `core/`: shared domain primitives, enums, identifiers, and error types
- `shared/`: shared config and utility packages
- `infrastructure/`: DB, Redis, queue, and chain integration placeholders
- `modules/`: feature/domain modules aligned with the planning pack

